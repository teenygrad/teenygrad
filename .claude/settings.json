{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "d=$(jq -r '.cwd // empty'); b=$(git -C \"${d:-.}\" branch --show-current 2>/dev/null); if [ \"$b\" = \"main\" ] || [ \"$b\" = \"master\" ]; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"BLOCKED: this repository does not allow edits on the default branch. Create a feature/ or release/ branch first (e.g. git checkout -b feature/<name>), then retry.\"}}'; fi; exit 0",
            "statusMessage": "Checking branch"
          }
        ]
      }
    ]
  }
}
